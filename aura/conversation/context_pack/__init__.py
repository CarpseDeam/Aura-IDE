"""Shared, read-only context-pack building blocks (budget, file, test, dependency).

Used by the production ``read_task_context`` tool. The Worker Context Pack
orchestrator was removed with the dispatch architecture.
"""
